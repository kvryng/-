# Проектирование и реализация конвейера обработки данных для задачи анализа рынка труда в арктических регионах России

## 🔧 Требования

- Windows 10/11
- Python 3.8+
- Java 11
- Доступ к интернету

##  Установка и настройка

### 1. Скачайте и установите Apache Spark

1. Скачайте архив **spark-3.5.0-bin-hadoop3.tgz** с [официального сайта](https://spark.apache.org/downloads.html)
2. Распакуйте содержимое в папку:  C:\spark\

### 2. Проверьте версию Java

Откройте командную строку и выполните:
```cmd
java -version 
```
Должно отобразиться:
```openjdk version "11.x.x" ...```

### 3. Установите Python-библиотеки
#### Основные зависимости
```cmd
pip install pyspark streamlit pymongo requests pandas pyarrow
```
#### PySpark 3.5
```cmd
pip install pyspark==3.5.0
```

### 4. Запуск проекта
#### Вариант A: Через командную строку
```cmd
cd C:\projects\arctic_vacancies
streamlit run dash.py
```

#### Вариант B: Через bat-файл
Дважды кликните по файлу
```start.bat``` в папке проекта.

### 5. Работа с данными
Запустите парсинг данных (вручную или через Планировщик задач)
Обновите страницу дашборда в браузере
Данные автоматически загрузятся из файла ```data/superset/arctic_vacancies.parquet```

## Структура проекта
```
projects/
└── arctic_vacancies/
    ├── kurs.py # Основной скрипт парсинга данных с hh.ru
    ├── dash.py # Streamlit-дашборд для визуализации
    ├── start.bat # Скрипт запуска дашборда
    └── data/
        └── superset/
            └── arctic_vacancies.parquet # Обработанные данные в формате Parquet
```
